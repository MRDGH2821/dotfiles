---
flatpak:
  - app.grayjay.Grayjay
  - com.github.tchx84.Flatseal
  - com.jeffser.Alpaca
  - io.ente.auth
  - io.github.flattool.Warehouse
  - org.gnome.Firmware
  - org.gnome.Snapshot
  - org.gramps_project.Gramps
installer_url:
  - if ! command -v calibre >/dev/null 2>&1; then sudo -v && wget -nv -O- https://download.calibre-ebook.com/linux-installer.sh | sudo sh /dev/stdin; fi
  - if ! command -v oh-my-posh >/dev/null 2>&1; then curl -s https://ohmyposh.dev/install.sh | bash -s -- -d ~/.local/bin ; else oh-my-posh upgrade --force; fi
  - if ! command -v zed >/dev/null 2>&1; then curl -f https://zed.dev/install.sh | sh; fi
kde:
  - discover
  - filelight
  - kcalc
  - kclock
  - partitionmanager
  - skanlite
  - skanpage
packages:
  - btop
  - direnv
  - firefox
  - flatpak
  - fzf
  - git
  - git-credential-oauth
  - git-delta
  - keepassxc
  - kleopatra
  - mangohud
  - rustup
  - shfmt
  - syncthing
  - vlc
  - zoxide
  - zsh
tablet_only:
  - iio-sensor-proxy
  - waydroid
  - waydroid-helper
