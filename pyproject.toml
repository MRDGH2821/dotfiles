[project]
name = "dotfiles"
version = "0.1.0"
description = "This is my dotfiles repo managed using chezmoi."
readme = "README.md"
requires-python = ">=3.13"

[dependency-groups]
dev = ["pyyaml>=6.0.3", "rich>=14.3.3"]
